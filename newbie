const express = require('express');
const app = express();
const port = 808;

app,get('/', (req, res) => {
  res.send('saya sudah belajar github');
});

app,listen(port, () => {
  console.log(`server berjalan di http://localhost:${port}`);
});
